# Sample App

A deliberately small application used as an indexing fixture. It contains an
import cycle, an unreferenced function, an unused dependency and a planted
credential so the health and redaction checks have something real to find.

## Setup

Run `npm install`.
